name: common_plugin_csx_example
description: Demonstrates how to use the common_plugin_csx plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  common_plugin_csx:
    path: ../
  image_picker: ^0.7.5+2 # 获取本地图片
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
